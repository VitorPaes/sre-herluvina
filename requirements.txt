# requirements.txt
#
# Riscos:
# 1. Incompatibilidades de dependências transitivas entre dbt-core (ex: dependências de Jinja2/click) e Streamlit.
# 2. Atualizações menores de dbt-duckdb que introduzam quebras na extensão httpfs do DuckDB.
# 3. Demora excessiva no build do container se o pyarrow exigir compilação a partir de código fonte C++ no host local.
#
# Ambiguidades:
# 1. Se a versão estável do duckdb (0.10.x) no python-duckdb é idêntica à suportada pelo dbt-duckdb 1.7.x.
# 2. Se a biblioteca de renderização gráfica do plotly exige pacotes adicionais do sistema operacional Debian.

dbt-duckdb>=1.7.0
duckdb>=0.10.0
boto3>=1.34.0
streamlit>=1.30.0
pandas>=2.1.0
pyarrow>=14.0.0
plotly>=5.18.0
